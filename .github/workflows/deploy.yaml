name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Prepare SSH keys
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SERVER_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p ${{ secrets.SERVER_PORT }} ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

          # Deploy key для доступа к репозиторию (если он приватный)
          echo "${{ secrets.REPO_DEPLOY_KEY }}" > ~/.ssh/repo_key
          chmod 600 ~/.ssh/repo_key

      - name: SSH into server and deploy
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no \
            -p ${{ secrets.SERVER_PORT }} \
            ${{ secrets.SERVER_USERNAME }}@${{ secrets.SERVER_HOST }} << 'EOF'
            set -e

            # Настройка SSH для git (если репозиторий приватный)
            mkdir -p ~/.ssh
            echo "${{ secrets.REPO_DEPLOY_KEY }}" > ~/.ssh/id_rsa
            chmod 600 ~/.ssh/id_rsa
            ssh-keyscan github.com >> ~/.ssh/known_hosts

            # Клонирование или обновление репозитория
            cd ~
            if [ ! -d MasikServer ]; then
              GIT_SSH_COMMAND="ssh -i ~/.ssh/id_rsa" git clone git@github.com:Pie-Roman/MasikServer.git MasikServer
            else
              cd MasikServer
              GIT_SSH_COMMAND="ssh -i ~/.ssh/id_rsa" git pull origin main
            fi

            cd MasikServer

            # Установка Docker если отсутствует
            if ! command -v docker > /dev/null 2>&1; then
              echo "Installing Docker..."
              curl -fsSL https://get.docker.com -o get-docker.sh
              sudo sh get-docker.sh
            fi

            # Установка Docker Compose если отсутствует
            if ! command -v docker-compose > /dev/null 2>&1; then
              echo "Installing Docker Compose..."
              sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
              sudo chmod +x /usr/local/bin/docker-compose
            fi

            # Запуск dockerd, если демон не активен
            if ! sudo docker info > /dev/null 2>&1; then
              echo "Starting Docker daemon..."
              sudo nohup dockerd > /dev/null 2>&1 &
              for i in {1..10}; do
                sleep 2
                if sudo docker info > /dev/null 2>&1; then
                  echo "Docker started"
                  break
                fi
              done
            fi

            # Docker Compose restart
            sudo docker-compose down || true
            sudo docker-compose pull || true
            sudo docker-compose up -d
          EOF
